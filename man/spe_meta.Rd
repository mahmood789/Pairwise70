% Generated by roxygen2: do not edit by hand, % Please edit documentation in R/advanced_pooling_v4.R
\name{spe_meta}
\alias{spe_meta}
\title{Stochastic Point Estimator (SPE)}
\usage{
spe_meta(yi, vi, n_samples = 10000, burnin = 1000,
  tau_prior = "half_cauchy")
}
\arguments{
\item{yi}{Numeric vector of effect sizes}

\item{vi}{Numeric vector of sampling variances}

\item{n_samples}{Number of MCMC samples (default 10000)}

\item{burnin}{Burn-in period (default 1000)}

\item{tau_prior}{Prior for tau² (default "half_cauchy")}
}
\value{
List with estimate, se, ci_lb, ci_ub, pvalue, and diagnostics
}
\description{
Quantifies estimation uncertainty via stochastic sampling of tau².
Treats tau² as a random variable with posterior distribution, samples
from the posterior using Metropolis-Hastings, then computes pooled
estimate for each sample. Final estimate is median, SE is std dev of samples.
}
\examples{
\dontrun{
yi <- c(0.25, 0.30, 0.28, 0.35, 0.22)
vi <- c(0.02, 0.01, 0.02, 0.01, 0.03)
result <- spe_meta(yi, vi, n_samples = 5000, burnin = 500)
print(result$accept_rate)
}
}
\references{
Gelman, A. (2006). Prior distributions for variance parameters in
hierarchical models. Bayesian Analysis, 1(3), 515-534.
}
\keyword{bayesian}
\keyword{mcmc}
