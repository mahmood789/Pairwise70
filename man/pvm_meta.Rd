% Generated by roxygen2: do not edit by hand, % Please edit documentation in R/advanced_pooling_v4.R
\name{pvm_meta}
\alias{pvm_meta}
\title{Profile Variance Minimization (PVM)}
\usage{
pvm_meta(yi, vi, tau_grid_min = 0, tau_grid_max = 1,
  n_grid_points = 100, coverage_target = 0.95, n_boot = 500)
}
\arguments{
\item{yi}{Numeric vector of effect sizes}

\item{vi}{Numeric vector of sampling variances}

\item{tau_grid_min}{Minimum tau² for grid (default 0)}

\item{tau_grid_max}{Maximum tau² for grid (default 1)}

\item{n_grid_points}{Number of grid points (default 100)}

\item{coverage_target}{Target coverage (default 0.95)}

\item{n_boot}{Bootstrap iterations for coverage (default 500)}
}
\value{
List with estimate, se, ci_lb, ci_ub, pvalue, and diagnostics
}
\description{
Choose tau² that directly optimizes CI properties. Profiles over a grid
of tau² values, estimates empirical coverage for each via bootstrap,
and selects the tau² that achieves target coverage with minimum CI width.
}
\examples{
\dontrun{
yi <- c(0.25, 0.30, 0.28, 0.35, 0.22)
vi <- c(0.02, 0.01, 0.02, 0.01, 0.03)
result <- pvm_meta(yi, vi, n_grid_points = 50, n_boot = 200)
print(result$tau2_optimal)
}
}
\keyword{coverage}
\keyword{optimization}
